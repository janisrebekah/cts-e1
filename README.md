# Smart Restock Inventory Alert System

Project scaffold for a React + Vite frontend and Python + FastAPI backend.

Business logic and database operations belong in the backend. The frontend should communicate with the backend through REST APIs only.
